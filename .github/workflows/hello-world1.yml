name: hello-world
on:
  push:
    branches: [master]
  workflow_dispatch:
jobs:
  say-hello:
    name: print hello world
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello world"
